{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability set for sik-render",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "shell:allow-open",
    "fs:read-all",
    "fs:write-all",
    "dialog:allow-open",
    "dialog:allow-save",
    "notification:default"
  ]
}
